[tool:pytest]
# Fast pytest configuration for development
addopts =
    -v
    --tb=short
    --no-cov
    --disable-warnings
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*